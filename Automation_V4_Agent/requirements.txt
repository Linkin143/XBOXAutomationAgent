# ============================================================
# Automation V4 Agent — Python Dependencies
# Replaces all Hcl.eDAT.*.dll and PowerShell eDAT framework
# ============================================================

# --- Core AI Agent Stack ---
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langgraph>=0.1.0

# --- LiteLLM: unified LLM router (GPT-4o ↔ Claude Sonnet, zero code change) ---
litellm>=1.40.0

# --- Hardware: Serial / UART / Arduino ---
pyserial>=3.5

# --- Vision: Image Capture + Processing ---
opencv-python>=4.9.0
Pillow>=10.0.0
numpy>=1.26.0

# --- Vision: OCR (replaces Hcl.eDAT.OCR.dll + Tesseract .NET) ---
pytesseract>=0.3.10

# --- Vision: Screen capture fallback (Windows) ---
pyautogui>=0.9.54
mss>=9.0.1

# --- Reporting: Word doc generation (replaces Hcl.eDAT.WordReport.dll) ---
python-docx>=1.1.0

# --- Configuration ---
pyyaml>=6.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# --- Windows-specific: COM/IR/Video ---
pywin32>=306; sys_platform == "win32"

# --- Async & Utilities ---
asyncio-throttle>=1.0.2
typing-extensions>=4.9.0
dataclasses-json>=0.6.0

# --- Logging ---
loguru>=0.7.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
